using System;
using System.IO;
using System.Text;

namespace AnimalInfo
{
    class Program
    {
        static void Main(string[] args)
        {
            //using (FileStream fs = new FileStream("C:/Users/fhfht/source/repos/AnimalInfo/test.txt", FileMode.Open, FileAccess.Write))
            //{
            //    StreamWriter sw = new StreamWriter(fs, Encoding.UTF8);

            //    //sw.WriteLine(listViewItem.Text);
            //    //sw.WriteLine(listViewItem1.Text);
            //    sw.WriteLine("test message");
            //}

            //int value = 1123122; float value2 = 3.14f; string str1 = "Hello World! asdadada"; 
            ////FileStream fs = new FileStream("test.txt", FileMode.Create); StreamWriter sw = new StreamWriter(fs); 
            ////sw.Write(value); sw.Write(value2); sw.Write(str1); sw.Close(); //방법2 
            //using (StreamWriter sww = new StreamWriter(new FileStream("C:/Users/fhfht/source/repos/AnimalInfo/test.txt", FileMode.Append))) 
            //{ 
            //    sww.WriteLine(value); 
            //    sww.WriteLine(value2); 
            //    sww.WriteLine(str1); 
            //}

          

        }
    }
}
